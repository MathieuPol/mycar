<form method="post" action="{{ path('app_back_brand_delete', {'id': brand.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cette marque?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-danger me-2">Supprimer la marque</button>
</form>
