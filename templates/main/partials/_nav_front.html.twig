   <div class="banner row container-fluid p-0 m-0" role="banner">
        <img class="p-0 m-0" src=" {{ asset('img/ProjetMycar.svg' )}}" alt="">
    </div>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="{{ path('fronthome') }}">Project MyCar</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('fronthome') }}">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('newCar') }}">Neuves</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href=" {{ path('usedCar') }} ">Occasions</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href=" {{ path('description') }}">Description</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('app_api_car')}}">Api</a>
              </li>

          
            {% if is_granted('ROLE_MANAGER') %}
            
              <li class="nav-item">
                <a class="nav-link" href="{{ path('app_back_main')}}">Back-office</a>
              </li>
            {% else %}
            {% endif %}
              
             
            </ul>

                  {% if app.user != null %}

                    <button onclick="window.location.href= '{{ path('app_logout')}}'" type="button" class="btn btn-danger me-2">Déconnexion</button>

                  {% endif %}

                  {% if app.user == null %}
                                <button onclick="window.location.href= '{{ path('app_login')}}'" type="button" class="btn btn-success me-2">Connexion</button>
                  {% endif %}


          </div>
        </div>
      </nav>